apiVersion: v1
kind: Service
metadata:
  name: {{ include "deno-web-app.fullname" . }}
  labels:
    {{- include "deno-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deno-web-app.selectorLabels" . | nindent 4 }}
