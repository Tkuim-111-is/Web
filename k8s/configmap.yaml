apiVersion: v1
kind: ConfigMap
metadata:
  name: deno-web-app-config
data:
  # 應用配置
  PORT: "8000"
  HOST: "0.0.0.0"
  # 資料庫配置（非敏感資訊）
  DB_HOST: "localhost"
  DB_NAME: "deno_web_db"
  DB_PORT: "5432"
