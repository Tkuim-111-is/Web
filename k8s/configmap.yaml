apiVersion: v1
kind: ConfigMap
metadata:
  name: deno-web-app-config
data:
  # 在這裡添加您的配置
  PORT: "8000"
  HOST: "0.0.0.0"
  # 添加缺少的環境變數
  JWT_SECRET: "your-jwt-secret-key-here"
  DB_HOST: "localhost"
  DB_NAME: "deno_web_db"
  DB_PASS: "db_password"
  DB_PORT: "5432"
